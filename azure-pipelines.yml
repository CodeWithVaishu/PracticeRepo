# New pipeline
# Vaishnavi

trigger:
- none

variables:
  workingDirectory: 'terraform'

pool:
  name: 'Default'

stages: 

#stage1

- stage: Validate
  displayName: Validate Terraform
  jobs: 
  - job: TerraformValidate
    steps:
    - checkout: self

    - task: TerraformInstaller@1
      displayName: Install Terraform
      inputs: 
        terraformVersion: '1.5.7'

    - task: TerraformTask@5
      displayName: Terraform Init
      inputs:
        provider: 'azurerm'
        backendServiceArm: 'AzureConnection6'
        backendAzureRmOverrideSubscriptionID: 'af43aaaf-c72d-456d-a758-b503ecbac85f'
        backendAzureRmResourceGroupName: 'Vaishnavi-Practice'
        backendAzureRmStorageAccountName: 'vaishnaviterraform'
        backendAzureRmContainerName: 'state1'
        backendAzureRmKey: 'pipeline.tfstate'

    - script: |
       terraform fmt -check -recursive
      displayName: Terraform Format Check

    - task: TerraformTask@5
      displayName: 'Terraform Validate'
      inputs:
        provider: 'azurerm'
        command: 'validate'
        workingDirectory: $(workingDirectory)/terraform

# Stage2: Plan
- stage: Plan
  displayName: Terraform Plan
  dependsOn: Validate
  jobs:
  - job: TerraformPlan
    steps: 
    - checkout: self

    - task: TerraformInstaller@1
      displayName: Install Terraform
      inputs:
        terraformVersion: '1.5.7'

    - task: TerraformTask@5
      inputs:
        provider: 'azurerm'
        command: 'init'
        backendServiceArm: 'AzureConnection6'
        backendAzureRmOverrideSubscriptionID: 'af43aaaf-c72d-456d-a758-b503ecbac85f'
        backendAzureRmResourceGroupName: 'Vaishnavi-Practice'
        backendAzureRmStorageAccountName: 'vaishnaviterraform'
        backendAzureRmContainerName: 'state1'
        backendAzureRmKey: 'pipeline.tfstate'

    - task: Terraformtask@5
      displayName: 'Terraform Plan'
      inputs:
        command: 'plan'
        workingDirectory: $(workingDirectory)/terraform
        environmentServiceNameAzureRM: 'AzureConnection6'
        backendAzureRmOverrideSubscriptionID: 'af43aaaf-c72d-456d-a758-b503ecbac85f'
        commandOptions: "-out=tfplan"
      
    - publish: $(workingDirectory)/tfplan
      artifact: tfplan
